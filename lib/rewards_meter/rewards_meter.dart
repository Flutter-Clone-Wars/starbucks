import 'package:flutter/material.dart';

// TODO: implement the full rewards meter (all we have now are the stars)
class RewardsMeter extends StatefulWidget {
  const RewardsMeter({Key? key}) : super(key: key);

  @override
  _RewardsMeterState createState() => _RewardsMeterState();
}

class _RewardsMeterState extends State<RewardsMeter> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
